function addTogether(...args) {
  if(args.every(item => typeof item === "number")){
    if(args.length > 1){    
      return args.reduce((a, b) => a + b);
    } else {
      return sum += args;
    }
  }

  return undefined;
}

addTogether(2, 3);
